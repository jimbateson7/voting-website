type Event @model @auth(rules: [{allow: public}]) {
  id: ID!
  userGuid: ID
  name: String
  attribues: AWSJSON
}

enum Choice {
  YES
  NO
}

type Vote @model @auth(rules: [{allow: public}]) {
  id: ID!
  voterId: ID
  choice: Choice!
}
 